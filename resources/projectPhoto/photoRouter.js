// const express = require('express');
// const router = express.Router();
// const photoController = require('./photoController');

// const secured = require('../utils/secured');

// router.post('/signed', photoController.signedUrl);
// // router.get('/', photoController.getAllPhotos);
// router.get('/:id', photoController.getPhotosByProjectId);
// router.get('/one/:id', photoController.getPhotoById);
// router.post('/', secured, photoController.createProjectPhoto);
// router.delete('/:id', secured, photoController.deletePhotoById);

// module.exports = router;
